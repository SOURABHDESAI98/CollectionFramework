package mapInterface;

public class MapRules {

	/*
	 * 
	 * 1. map is not a subtype in collection interface , it's seperate interface
	 * 
	 * 2. whenever we want to store data in key-value pairs then we use map
	 * interface
	 * 
	 * 3. keys can not be duplicate but values can be duplicate
	 * 
	 * 4. only one null is allowed in keys, multiple null can be stored as values
	 * 
	 * 5. Wrapper classes and Custom Classes can be stored as key-value pairs
	 * 
	 * 6. each key-value pair is known as entry
	 * 
	 * 7. it stores key-value pairs in curly bracket
	 * 
	 * 8.you can also add collection as a key or value in hashMap
	 * 
	 * eg- <Integer,List<>>, <Integer,ArrayList<>>,
	 * <Integer,Set<>>,<Integer,LinkedList<>>
	 * 
	 */

	public static void main(String[] args) {

	}

}
