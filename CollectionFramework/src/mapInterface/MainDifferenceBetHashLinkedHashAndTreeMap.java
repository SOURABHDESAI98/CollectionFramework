package mapInterface;

public class MainDifferenceBetHashLinkedHashAndTreeMap {

	/*
	 * 
	 * 1.HashMap- it does not follow insertion order and follows hashtable data
	 * structure, null value will be placed at 0th index
	 * 
	 * 2. LinkedHashMap- it follows insertion order and follows linkedlist+hashtable
	 * data structure, null value will not be placed at 0th index
	 * 
	 * 3. TreeMap- it sorts keys ascendingly by default, doesn't take null as key,
	 * to sort keys who are objects of custom class then implement comparable,
	 * comparator interface
	 */
	// some methods of them may differ
	// rest of the things are same

	public static void main(String[] args) {

	}

}
