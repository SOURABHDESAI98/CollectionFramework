package collectionFramework.list.linkedList;

//	LinkedList implements the doubly linked list of the list interface.

public class Types {

	/*
	 * 1. singly linked list-
	 * 
	 * # it has only one node (next node) having address of next element
	 * 
	 * # tail element node has null value
	 * 
	 * 
	 * 
	 * 2. doubly linked list
	 * 
	 * # it has two nodes (next node & previous nodes) having address of next
	 * element and previous node
	 * 
	 * # previous node of head element and next node of tail element has null value
	 * 
	 * 
	 * 
	 * 3. circular linked list
	 * 
	 * # next node of tail has address of head element and previous node of head
	 * element has address of tail element
	 *
	 */

	public static void main(String[] args) {

	}

}
